import { dev } from '$app/environment'

export const title = 'brocks blog'
export const description = 'brocks personal blogx'
export const url = dev ? 'http://localhost:5173/' : 'https://brocke.xyz/'

export const categories = ['math', 'pyrometer', 'interval analysis']
